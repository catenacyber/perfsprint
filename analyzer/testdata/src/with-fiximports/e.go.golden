package a

import (
	// want "fiximports: Fix imports"
)

func fmte() {
	// fmt removed as only import
	println("Hello, World!", "toto") // want "string-format: fmt.Sprintf can be replaced with just using the string"
}
