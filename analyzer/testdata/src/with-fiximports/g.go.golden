package a

import ( // want "fiximports: Fix imports"
	"fmt"
	"strconv"
)

func fmtg() {
	// fmt still used after removal, but new strconv
	fmt.Printf("Hello, World!", strconv.Itoa(123)) // want "integer-format: fmt.Sprintf can be replaced with faster strconv.Itoa"
}
